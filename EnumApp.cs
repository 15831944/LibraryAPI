using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace LibraryAPI
{
    public class EnumApp
    {
        //public string[] LoopThroughEnum(Enum eu)
        //{
        //    List<string> temp = new List<string>();
        //    foreach (Enum it in Enum.GetValues(typeof(eu)))
        //    {
        //        temp.Add(it.ToString());
        //    }

        //    return temp.ToArray();
        //}
    }
}
